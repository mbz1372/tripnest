import Link from 'next/link';import {Star, MapPin} from 'lucide-react';
export function HotelCard({h,locale}:{h:any,locale:string}){const name=locale==='fa'?h.nameFa:h.nameEn, city=locale==='fa'?h.cityFa:h.cityEn;return <Link href={`/${locale}/hotels/${h.id}`} className="glass block overflow-hidden rounded-[2rem] transition hover:-translate-y-1"><div className="h-44 bg-gradient-to-br from-blue-500/20 via-emerald-500/20 to-amber-300/30 p-4"><div className="flex justify-between"><span className="rounded-full bg-white/80 px-3 py-1 text-xs font-bold">{h.provider}</span><span className="rounded-full bg-slate-950 px-3 py-1 text-xs font-bold text-white">{h.score}</span></div></div><div className="p-5"><h3 className="text-xl font-black">{name}</h3><p className="mt-2 flex items-center gap-2 text-slate-500"><MapPin size={16}/>{city} — {h.landmark}</p><div className="my-4 flex gap-1">{Array.from({length:h.stars}).map((_,i)=><Star key={i} size={16} className="fill-amber-400 text-amber-400"/>)}</div><div className="flex items-end justify-between"><div><p className="text-xs text-slate-500">{locale==='fa'?'شروع قیمت':'From'}</p><b className="text-2xl">{(h.price/1000000).toFixed(1)}M</b></div><span className="rounded-full bg-emerald-500/10 px-3 py-1 text-sm font-bold text-emerald-700">{h.rooms} rooms</span></div></div></Link>}
