import Link from 'next/link';import {t} from '@/lib/i18n';
export function Nav({locale}:{locale:string}){const d=t(locale);const items=[['',d.home],['hotels',d.hotels],['flights',d.flights],['tours',d.tours],['planner',d.planner],['guides',d.guides],['travel-mate',d.mate],['blog',d.blog],['admin',d.admin]];return <nav className="sticky top-0 z-50 border-b border-black/10 bg-white/70 backdrop-blur-2xl dark:bg-slate-950/70"><div className="page flex items-center justify-between gap-4 py-3"><Link href={`/${locale}`} className="flex items-center gap-3 font-black"><span className="grid h-10 w-10 place-items-center rounded-2xl lux-grad text-white">T</span><span>TripNest Enterprise</span></Link><div className="hidden items-center gap-1 lg:flex">{items.map(([href,label])=><Link className="rounded-full px-3 py-2 text-sm hover:bg-slate-100 dark:hover:bg-white/10" href={`/${locale}/${href}`.replace(/\/$/,'')} key={href}>{label}</Link>)}</div><div className="flex gap-2"><Link className="chip text-sm" href={locale==='fa'?'/en':'/fa'}>{locale==='fa'?'EN':'FA'}</Link><Link className="rounded-full bg-slate-950 px-4 py-2 text-sm text-white dark:bg-white dark:text-slate-950" href={`/${locale}/admin`}>Admin</Link></div></div></nav>}
