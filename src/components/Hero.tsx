import Link from 'next/link';import {Plane,MapPinned,Sparkles} from 'lucide-react';import {dict,Locale,cities} from '@/lib/data';
export function Hero({locale}:{locale:Locale}){const d=dict[locale];return <section className="containerx py-16"><div className="grid lg:grid-cols-2 gap-8 items-center"><div><div className="btn btn-soft mb-5"><Sparkles size={18}/>Enterprise Offline V1</div><h1 className="text-4xl md:text-6xl font-black leading-tight text-brand-900">{d.heroTitle}</h1><p className="mt-6 text-lg text-slate-600 leading-9">{d.heroSub}</p><div className="mt-8 flex gap-3 flex-wrap"><Link className="btn btn-primary" href={`/${locale}/itinerary`}><MapPinned size={18}/>{d.cta}</Link><Link className="btn btn-soft" href={`/${locale}/hotels`}><Plane size={18}/>Explore hotels</Link></div></div><div className="card p-6"><div className="rounded-3xl bg-gradient-to-br from-brand-100 to-emerald-50 p-6"><p className="font-black text-brand-900 mb-5">Popular destinations</p><div className="grid grid-cols-2 gap-3">{cities.map(c=><div className="bg-white rounded-2xl p-4" key={c.name}><b>{locale==='fa'?c.name:c.en}</b><p className="text-sm text-slate-500">{c.hotels} hotels · {c.tag}</p></div>)}</div></div></div></div></section>}
