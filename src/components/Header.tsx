import Link from 'next/link';import {dict,Locale} from '@/lib/data';
export function Header({locale}:{locale:Locale}){const d=dict[locale];const links=['hotels','itinerary','guides','travel-mate','blog','admin'];return <header className="sticky top-0 z-20 glass"><div className="containerx flex items-center justify-between py-4"><Link href={`/${locale}`} className="text-2xl font-black text-brand-900">TripNest</Link><nav className="hidden md:flex gap-5 text-sm font-bold">{links.map((l,i)=><Link key={l} href={`/${locale}/${l}`}>{d.nav[i]}</Link>)}</nav><div className="flex gap-2"><Link className="btn btn-soft" href={locale==='fa'?'/en':'/fa'}>{locale==='fa'?'EN':'FA'}</Link><Link className="btn btn-primary" href={`/${locale}/admin`}>{d.admin}</Link></div></div></header>}
