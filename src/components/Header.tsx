import Link from 'next/link';import {dict,Locale,nav} from '@/lib/data';
export function Header({locale}:{locale:Locale}){const d=dict[locale];return <header className="sticky top-0 z-50 glass"><div className="containerx flex items-center justify-between py-4"><Link href={`/${locale}`} className="text-2xl font-black text-navy">TripNest<span className="text-ocean">.</span></Link><nav className="hidden lg:flex gap-5 text-sm font-black">{nav.map(([href,fa,en])=><Link key={href} href={`/${locale}/${href}`}>{locale==='fa'?fa:en}</Link>)}</nav><div className="flex gap-2"><Link className="btn btn-soft" href={`/${locale}/profile`}>Profile</Link><Link className="btn btn-soft" href={locale==='fa'?'/en':'/fa'}>{d.lang}</Link><Link className="btn btn-primary" href={`/${locale}/admin`}>{d.admin}</Link></div></div></header>}
