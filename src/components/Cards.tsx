import Link from 'next/link';import {ArrowLeft,Star} from 'lucide-react';import {hotels,money} from '@/lib/data';
export function HotelCard({h}:{h:typeof hotels[number]}){return <div className="card p-5"><div className="flex justify-between gap-3"><div><b className="text-xl text-navy">{h.name}</b><p className="muted mt-1">{h.city} · {h.grade} · {h.provider}</p></div><span className="chip"><Star size={14}/>{h.score}</span></div><div className="mt-5 flex flex-wrap gap-2">{h.tags.map(x=><span className="chip" key={x}>{x}</span>)}</div><div className="mt-6 flex items-center justify-between"><div><p className="muted text-sm">شروع قیمت</p><b className="text-navy">{money(h.price)}</b></div><Link className="btn btn-primary" href="./checkout">رزرو</Link></div></div>}
export function ModuleCard({title,sub,href,icon}:{title:string,sub:string,href:string,icon:React.ReactNode}){return <Link href={href} className="card p-6 block hover:shadow-card transition"><div className="w-12 h-12 rounded-2xl bg-sky text-ocean grid place-items-center">{icon}</div><b className="block mt-5 text-xl text-navy">{title}</b><p className="muted mt-2 leading-7">{sub}</p><span className="mt-5 inline-flex items-center gap-2 font-black text-ocean">مشاهده <ArrowLeft size={16}/></span></Link>}
