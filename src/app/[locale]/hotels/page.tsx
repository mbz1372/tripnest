import {PageHero} from '@/components/PageHero';import {hotels,money,Locale} from '@/lib/data';import Link from 'next/link';
export default function Hotels({params}:{params:{locale:Locale}}){return <><PageHero title="Hotel Marketplace" sub="لیست هتل‌ها با فیلتر شهر، گرید، قیمت، امتیاز، پروایدر، وضعیت آنلاین و قابلیت اتصال به موتور رزرو."/><section className="containerx section"><div className="card p-5 grid md:grid-cols-5 gap-3"><input className="input md:col-span-2" placeholder="نام هتل یا شهر"/><select className="input"><option>همه شهرها</option><option>تهران</option><option>مشهد</option></select><select className="input"><option>همه پروایدرها</option><option>IHO Provider</option><option>NetMinder</option></select><button className="btn btn-primary">جستجو</button></div><div className="grid-auto mt-7">{hotels.map(h=><Link href={`/${params.locale}/checkout`} className="card p-6" key={h.name}><div className="flex justify-between gap-3"><b className="text-xl text-navy">{h.name}</b><span className="chip">{h.score}</span></div><p className="muted mt-2">{h.city} · {h.grade} · {h.provider}</p><p className="text-2xl font-black text-ocean mt-4">{money(h.price)}</p><div className="flex gap-2 flex-wrap mt-4">{h.features.map(f=><span className="chip" key={f}>{f}</span>)}</div><button className="btn btn-primary mt-5 w-full">رزرو و دریافت ووچر</button></Link>)}</div></section></>}
