import {hotels} from '@/lib/data';export default function Hotels(){return <section className="containerx py-12"><h1 className="text-4xl font-black text-brand-900 mb-8">هتل‌ها و اقامتگاه‌ها</h1><div className="grid md:grid-cols-2 gap-5">{hotels.map(h=><div className="card p-6" key={h.name}><div className="flex justify-between"><h2 className="text-2xl font-black">{h.name}</h2><span className="btn btn-soft">{h.score}</span></div><p className="text-slate-500 mt-2">{h.city} · {h.grade} · Provider: {h.provider}</p><p className="mt-5 font-black text-brand-900">از شبی {h.price} تومان</p><button className="btn btn-primary mt-5">مشاهده و رزرو</button></div>)}</div></section>}
