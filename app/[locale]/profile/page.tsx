import {Nav} from '@/components/Nav';
type PageProps={params:Promise<{locale:string;id:string}>};import {Section,Card} from '@/components/Section';import {t} from '@/lib/i18n';
export default async function Page(props: PageProps){const params=await props.params;const d=t(params.locale);return <main className={d.dir==='rtl'?'rtl':'ltr'}><Nav locale={params.locale}/><Section eyebrow="TripNest Module" title="User profile and loyalty"><div className="grid-auto"><Card><h3 className="text-xl font-black">Experience</h3><p className="mt-3 text-slate-500">Trips, wallet, vouchers, loyalty tier, RFNL segment, saved hotels and notifications.</p></Card><Card><h3 className="text-xl font-black">Operations</h3><p className="mt-3 text-slate-500">Trips, wallet, vouchers, loyalty tier, RFNL segment, saved hotels and notifications.</p></Card><Card><h3 className="text-xl font-black">Data/AI</h3><p className="mt-3 text-slate-500">Trips, wallet, vouchers, loyalty tier, RFNL segment, saved hotels and notifications.</p></Card></div></Section></main>}