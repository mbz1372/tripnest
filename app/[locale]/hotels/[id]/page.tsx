import {Nav} from '@/components/Nav';import {Card,Section} from '@/components/Section';import {hotels} from '@/lib/data';import {t} from '@/lib/i18n';import {MapPin,Star} from 'lucide-react';
export default function HotelDetail({params}:{params:{locale:string,id:string}}){const d=t(params.locale);const h=hotels.find(x=>x.id===params.id)||hotels[0];const name=params.locale==='fa'?h.nameFa:h.nameEn;return <main className={d.dir==='rtl'?'rtl':'ltr'}><Nav locale={params.locale}/><div className="page py-8"><div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><div className="glass h-[430px] rounded-[3rem] bg-gradient-to-br from-blue-500/20 via-emerald-500/20 to-amber-300/30 p-6"><span className="rounded-full bg-white/80 px-4 py-2 font-bold">AI Summary Ready</span></div><Card><p className="text-brand font-bold">{h.provider}</p><h1 className="mt-3 text-5xl font-black">{name}</h1><p className="mt-4 flex items-center gap-2 text-slate-500"><MapPin/> {params.locale==='fa'?h.cityFa:h.cityEn} — {h.landmark}</p><div className="my-5 flex gap-1">{Array.from({length:h.stars}).map((_,i)=><Star key={i} className="fill-amber-400 text-amber-400"/>)}</div><p className="leading-8 text-slate-600">AI خلاصه: این هتل برای سفرهای لوکس، کاری و خانوادگی مناسب است؛ امتیاز بالا، موقعیت خوب، ظرفیت محدود و Margin مناسب دارد.</p><div className="mt-6 grid grid-cols-3 gap-3"><span className="chip">Score {h.score}</span><span className="chip">Margin {h.margin}%</span><span className="chip">{h.rooms} Rooms</span></div><button className="mt-7 w-full rounded-2xl lux-grad p-4 font-black text-white">Reserve Now</button></Card></div></div><Section eyebrow="Hotel Intelligence" title="Rooms, pricing, reviews, policy and nearby attractions"><div className="grid-auto">{['Dynamic pricing chart','Room availability','Cancellation policy','Nearby landmarks','FAQ','User reviews'].map(x=><Card key={x}><h3 className="text-xl font-black">{x}</h3><p className="mt-3 text-slate-500">API-ready enterprise OTA section.</p></Card>)}</div></Section></main>}