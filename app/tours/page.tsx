import Link from 'next/link';import {Clock,MapPin} from 'lucide-react';import {tours} from '@/lib/data';
export default function ToursPage(){return <main dir="rtl"><section className="pageHero"><div className="container"><h1>همه تورها</h1><p>لیست کامل پکیج‌های داخلی و خارجی تریپ‌جت</p></div></section><section className="section"><div className="container"><div className="cards">{tours.map(t=><Link className="card" href={`/tours/${t.slug}`} key={t.slug}><div className="photo" style={{backgroundImage:`url(${t.img})`}}><span className="discount">{t.tag}</span></div><div className="cardbody"><h3>{t.title}</h3><div className="meta"><span><MapPin size={14}/> {t.country}</span><span><Clock size={14}/> {t.days}</span></div><div className="price">از {t.price} تومان</div></div></Link>)}</div></div></section></main>}
